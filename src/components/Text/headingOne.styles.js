import styled from "styled-components";

export const HOne = styled.h2`
  font-size: 2.8rem;
  font-weight: 700;
  margin-bottom: 0.8rem;
  text-transform: uppercase;
  color: var(--primary);
  text-align: center;
`;
